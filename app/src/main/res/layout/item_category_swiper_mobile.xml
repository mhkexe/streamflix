<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_featured_mobile_card"
    android:clipToOutline="true">

    <ImageView
        android:id="@+id/iv_swiper_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:importantForAccessibility="no"
        tools:src="@drawable/detail_preview_backdrop" />

    <TextView
        android:id="@+id/tv_swiper_rating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/bg_featured_rating_liquid"
        android:paddingStart="10dp"
        android:paddingTop="6dp"
        android:paddingEnd="10dp"
        android:paddingBottom="6dp"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <View
        android:layout_width="match_parent"
        android:layout_height="125dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_detail_bottom_fade" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="6dp">

        <ImageView
            android:id="@+id/iv_swiper_logo"
            android:layout_width="180dp"
            android:layout_height="64dp"
            android:adjustViewBounds="true"
            android:scaleType="fitStart"
            android:visibility="visible"
            tools:src="@drawable/detail_preview_logo"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tv_swiper_title"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:visibility="gone" />

    </LinearLayout>
</FrameLayout>
